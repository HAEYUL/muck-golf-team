-- 기존 프로젝트를 위한 마이그레이션. 0001~0006을 먼저 실행했다는 전제로 실행하세요.
--
-- 정회원 탈퇴를 "완전 삭제"가 아니라 "비활성화"로 처리하기 위한 is_active 컬럼 추가.
-- 비활성화된 회원은 로그인 명단·참가체크·실력순위 목록에서는 빠지지만, 과거
-- 라운딩 기록(스코어·조편성 결과·추억사진 등)에 남아있는 이름은 그대로 보존된다.
-- 나중에 다시 들어오면 관리자가 다시 활성화로 되돌릴 수 있다.

alter table members add column if not exists is_active boolean not null default true;
